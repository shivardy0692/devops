ssh-keygen                               -     to generate a ssh key
scp test.java 192.168.1.1(dest_ip):/temp -     to copy a file from source to destination
scp -r home 192.168.1.1:/temp            -     copy a folder from source to destination
